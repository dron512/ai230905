<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">

	<select id="selectAll" parameterType="String" resultType="member.MemberDTO">
		select *
		from member
	</select>
	
	<insert id="insertMember" parameterType="member.MemberDTO">
		insert into member
		(
			member_loginId
			, member_password
			, member_name
			, member_tel
			, member_email
			, member_joinDate
			, member_category
		)
		values
		(
			#{member_loginId}
			, #{member_password}
			, #{member_name}
			, #{member_tel}
			, #{member_email}
			, NOW()
			, #{member_category}
		)
	</insert>
</mapper>